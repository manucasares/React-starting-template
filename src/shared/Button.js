import styled from 'styled-components';

export const Button = styled.button`
    background-color: violet;
    padding: .3rem 1.4rem;
    font-size: inherit;
    border-radius: 3px;
    border: none;
`